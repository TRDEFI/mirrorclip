import './style.css'

document.querySelector('#app').innerHTML = `
  <div class="container">
    <h1>Welcome to Your Web Project</h1>
    <p>Your development environment is now ready!</p>
    <div class="card">
      <p>Start building your application by editing the files in the <code>src</code> directory.</p>
    </div>
  </div>
`